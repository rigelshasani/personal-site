// Stub for react-markdown (ESM-only in v10, incompatible with Jest's CommonJS transform)
const ReactMarkdown = ({ children }) => children || null;
module.exports = ReactMarkdown;
module.exports.default = ReactMarkdown;
